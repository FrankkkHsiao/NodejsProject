<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="#"><img src="/img/bloodtrail.png" width="30" height="30" alt=""></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav  mr-auto">
                </li>
                <li class="nav-item  <%= (locals.pageName && pageName==='memberList') ? 'active' : '' %>">
                    <a class="nav-link" href="/memberList">資料列表</a>
                </li>
                <li class="nav-item  <%= (locals.pageName && pageName==='memberRegister') ? 'active' : '' %>">
                    <a class="nav-link" href="/memberRegister"><i class="fas fa-pen-nib"></i>新增資料</a>
                </li>
                <!-- <li class="nav-item">
                    <a class="nav-link" href="/#">b</a>
                </li> -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="/#" id="navbarDropdown" role="button"
                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        會員專區
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown  ">
                        <a class="dropdown-item" href="/memberEdit/<%= userSid %>">資料修改</a>
                        <a class="dropdown-item" href="/pwEdit/<%= userSid %>">密碼更改</a>
                    </div>
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
                <% if(locals.loginUser){ %>
                <span class="navbar-text">
                    Hello <%= loginUser %>,&emsp;</span>
                <a href="/logout">logout</a>
                <% } else { %>
                <span class="navbar-text">
                    Please login again,&emsp;</span>
                <a href="/login">login</a>
                <% } %>
            </form>
        </div>
    </div>
</nav>
<style>
    .navbar-nav>.nav-item.active {
        background-color: #868dff;
        color: white;
        border-radius: 10px;
    }
</style>